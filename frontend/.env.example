# lellostore Frontend Environment Configuration
# Copy this file to .env.local and configure for your environment

# OIDC Configuration (required for authentication)
# The issuer URL is the base URL of your OIDC provider
# Examples:
#   Keycloak: https://keycloak.example.com/realms/my-realm
#   Auth0: https://my-tenant.auth0.com
#   Okta: https://dev-12345.okta.com/oauth2/default
VITE_OIDC_ISSUER_URL=https://your-oidc-provider.com/realms/your-realm

# The client ID registered with your OIDC provider
VITE_OIDC_CLIENT_ID=lellostore-frontend

# API Configuration
# In production, leave empty (frontend and API served from same origin)
# In development, set to the backend URL (e.g., http://localhost:8080)
VITE_API_BASE_URL=
